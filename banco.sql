CREATE DATABASE bancotasks;
USE bancotasks;

/** Optei por criar a tabela utilizando o migrate do laravel
CREATE TABLE tasks(
codigo int(10) AUTO_INCREMENT,
titulo varchar(30) NOT NULL,
descricao varchar(50),
status varchar(10),
PRIMARY KEY (codigo)
);

*/
